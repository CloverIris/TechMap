{
  "name": "@seekstar/techmap-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@fastify/compress": "^8.3.0",
    "@seekstar/contract-runtime": "workspace:*",
    "@seekstar/core": "workspace:*",
    "@seekstar/storage-contract": "workspace:*",
    "@seekstar/storage-postgres": "workspace:*",
    "fastify": "^5.5.0"
  },
  "devDependencies": { "tsx": "^4.20.5" }
}
